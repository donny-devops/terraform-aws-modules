interface:
  display_name: "Terraform Aws Modules"
  short_description: "Repo-specific patterns and workflows for terraform-aws-modules"
  default_prompt: "Use the terraform-aws-modules repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true